SELECT id, name, description, DATE_FORMAT(releaseDate, "%Y-%M-%D") as releaseDate, filepath, avg
FROM movieTable INNER JOIN
(SELECT review, access, AVG(rating) AS avg,
FROM movieReviewTable
WHERE movieId=?) as movieAverage
WHERE id=?;
